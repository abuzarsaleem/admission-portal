/** Shared ACTIVE/INACTIVE status for master data entities. */
export enum MasterStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum DegreeLevel {
  Bachelor = 'Bachelor',
  Master = 'Master',
  Doctorate = 'Doctorate',
}
